<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="list_item_text_style" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="list_main_item_text_style" parent="list_item_text_style">
        <item name="android:paddingLeft">@dimen/list_item_text_padding</item>
        <item name="android:paddingRight">@dimen/list_item_text_padding</item>
        <item name="android:paddingTop">@dimen/list_item_text_padding</item>
        <item name="android:textColor">@color/list_main_text_color</item>
        <item name="android:textSize">@dimen/forecast_textSize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="list_child_item_text_style" parent="list_item_text_style">
        <item name="android:padding">@dimen/list_item_text_padding</item>
        <item name="android:textColor">@color/list_main_text_color</item>
        <item name="android:textSize">@dimen/forecast_child_textSize</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="list_view_style" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center</item>
        <item name="android:divider">@null</item>
    </style>

</resources>
